<?xml version="1.0" encoding="ISO-8859-1"?>
<!DOCTYPE pages SYSTEM "cellflash-1.3.dtd">
<pages descr="Confirm">
	<page nav="stop">
		Confirm<br/>
        To signup to {{ .service }} charging {{ .price }} per SMS. Please reply with YES to confirm or NO to decline
        <a href="{{ .url }}/v1/ussd/buy?slug={{ .slug }}&category={{ .category }}&package={{ .package }}">Yes</a>
        <a href="{{ .url }}/v1/ussd/buy?slug={{ .slug }}&category={{ .category }}&package={{ .package }}">No</a>
    </page>
</pages>